// String with input and sentence find lenght(means koto gula word + koto gula space asse)
#include<stdio.h>
int main()
{
    char str[20];
   gets(str);
   int len=strlen(str);// string er str formula diya shorte kora hoy,,,,ta na hole i+while diya sob korte hoy
   printf("%d",len);  // %d karon ekhane sonkha print korbo tai
    return 0;
}   
